# the-discrete-math-of-ip-addresses
Users input a 3-digit number four times, creating an IP address, and then the program will return the binary, octal and hexadecimal representations of the IP address.
